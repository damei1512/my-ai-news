name: Daily AI News Update

on:
  schedule:
    - cron: '0 0 * * *' # 每天 UTC 0 点运行
  workflow_dispatch:      # 允许手动运行

# 🔥 核心修复：直接在这里声明“我要写权限”
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0 # 获取完整的历史记录，防止推送到错误的节点
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        pip install feedparser google-generativeai
        
    - name: Run script
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: python auto_gen.py
      
    - name: Commit and push changes
      run: |
        git config --global user.name 'GitHub Action'
        git config --global user.email 'action@github.com'
        
        # 检查有没有生成 news.json，如果没有就不用提交了
        if [ -f "news.json" ]; then
            git add news.json
            
            # 检查文件是否有变化
            if git diff --staged --quiet; then
                echo "没有新新闻，跳过提交"
            else
                git commit -m "Auto update news: $(date +'%Y-%m-%d')"
                git push
            fi
        else
            echo "错误：news.json 没有生成！这才是可能是 API 的问题。"
            exit 1
        fi
